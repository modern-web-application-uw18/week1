const Timer = require('./Timer');
// import Timer from './Timer';

const countdown = new Timer(10);
countdown.start();

// Line 1 imports the timer class from Timer.js
// const countdown creates a new instance of the time object with 10 seconds called "countdown"
// the "countdown" object calls the .start() method which begins the countdown.
